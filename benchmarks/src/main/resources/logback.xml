<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <target>System.out</target>
        <encoder>
            <pattern>[%.-1level %-28logger{0} %d{HH:mm:ss.SSS}] %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Suppress logs for benchmarks - only show errors -->
    <root level="ERROR">
        <appender-ref ref="CONSOLE"/>
    </root>

    <!-- Specifically suppress EventProcessingEngine logs -->
    <logger name="sss.events.EventProcessingEngine" level="ERROR"/>
    <logger name="sss.events" level="ERROR"/>

</configuration>
